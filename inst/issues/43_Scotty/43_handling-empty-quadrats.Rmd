---
title: "tt_test() with data containing totally empty quadrats"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Jennifer Baltzer reported that `tt_test()` fails if the census data contains quadrates with cero species. What should the code do in such cases? Daniel Zuleta suggested that such quadrats should be filled with cero in the abundance of each species.

```{r}
load
```

